#Область ПрограммныйИнтерфейс

// Открывает печатную форму Уведомления о переходе на УСН с НПД
//
// Параметры:
//   Форма - ФормаКлиентскогоПриложения
//
Процедура УведомлениеОПереходенНаУСН(Форма) Экспорт
	
	Если Не ЗначениеЗаполнено(Форма.УведомлениеОПереходеНаУСН) Тогда
		// Не требуется
		Возврат;
	КонецЕсли;
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Ключ", Форма.УведомлениеОПереходеНаУСН);
	ПараметрыФормы.Вставить("ЗаполнитьПриОткрытии", Ложь);
	ПараметрыФормы.Вставить("UIDФорма1СОтчетность", ОбщегоНазначенияКлиентСервер.ПустойУникальныйИдентификатор());
	ПараметрыФормы.Вставить("ВидУведомления", НалогНаПрофессиональныйДоходКлиентСервер.ВидУведомленияПереходНПДУСН());
	
	ФормаОтчета = ПолучитьФорму(НалогНаПрофессиональныйДоходВызовСервера.ИмяФормыУведомлениеПереходСНПД(),
		ПараметрыФормы,
		Форма,
		Форма.Организация);
	
	РегламентированнаяОтчетностьКлиент.ВывестиМашиночитаемуюФормуУведомленияОСпецрежимах(ФормаОтчета);
	
КонецПроцедуры

// Вызывает исключение при возникновении ошибки при формировании заявления о переходе на УСН с НПД
//
// Параметры:
//   Форма - ФормаКлиентскогоПриложения
//
Процедура ПоказатьОшибкуФормированияЗаявления(КраткоеПредставлениеОшибки) Экспорт
	
	СтрокиОшибки = Новый Массив;
	СтрокиОшибки.Добавить(НСтр("ru = 'Ошибка при формировании заявления о переходе на УСН с НПД:'"));
	СтрокиОшибки.Добавить(КраткоеПредставлениеОшибки);
	
	ВызватьИсключение СтрСоединить(СтрокиОшибки, Символы.ПС);
	
КонецПроцедуры

#КонецОбласти