#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Возвращает строковое имя условия по ссылке
// на устаревшее перечисление УдалитьУсловияПримененияТребованийЗаконодательства.
//
// Предполагается, что имена условий перечислены в модуле УсловияПримененияТребованийЗаконодательства.
// Однако, функция этого не проверяет.
//
// Вызывает исключение, если передано несуществующее значение перечисления 
// (например, удаленное в конфигурации или от отключенного расширения конфигурации).
//
// Параметры:
//  Условие - ПеречислениеСсылка.УдалитьУсловияПримененияТребованийЗаконодательства
//  Кеш - Соответствие - должно быть инициализировано пустым соответствием
// 
// Возвращаемое значение:
//  Строка - имя условия
//
Функция ИмяУсловия(Условие, Кеш) Экспорт
	
	Если Не ЗначениеЗаполнено(Условие) Тогда
		Возврат "";
	КонецЕсли;
	
	Имя = Кеш[Условие];
	Если Имя <> Неопределено Тогда
		Возврат Имя;
	КонецЕсли;
	
	Имя = ОбщегоНазначения.ИмяЗначенияПеречисления(Условие);
	
	Кеш.Вставить(Условие, Имя);
	
	Возврат Имя;
	
КонецФункции

#КонецОбласти

#КонецЕсли
